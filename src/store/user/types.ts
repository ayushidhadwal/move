export type ChangeInfoPayload = {
  username: string;
  fullName: string;
  email: string;
};

export type PhonePayload = {
  phone: string;
};

export type imagePayload = {
  uri: string;
  name: string;
  type: string;
};
